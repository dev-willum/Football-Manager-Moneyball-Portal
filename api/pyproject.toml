[project]
name = "scoutview-api"
version = "0.1.0"
description = "Python serverless API for ScoutView"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"